spring:
  application:
    name: posts-service
  datasource:
    url: jdbc:postgresql://localhost:5433/postsDB
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true


server:
  port: 9010
  servlet:
    context-path: /posts